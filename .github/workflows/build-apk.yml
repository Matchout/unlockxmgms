name: Build API101 APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - ".github/workflows/build-apk.yml"

jobs:
  build:
    name: Build debug APK
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"

      - name: Generate Android project
        shell: bash
        run: |
          set -euo pipefail

          cat > settings.gradle <<'EOF_SETTINGS'
          pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }

          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories {
                  google()
                  mavenCentral()
              }
          }

          rootProject.name = "CnGmsUnlockerApi101"
          include ":app"
          EOF_SETTINGS

          cat > build.gradle <<'EOF_ROOT_GRADLE'
          plugins {
              id "com.android.application" version "8.7.3" apply false
          }
          EOF_ROOT_GRADLE

          mkdir -p app/src/main/java/io/github/cngmsunlocker
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/resources/META-INF/xposed

          cat > app/build.gradle <<'EOF_APP_GRADLE'
          plugins {
              id "com.android.application"
          }

          android {
              namespace "io.github.cngmsunlocker"
              compileSdk 35

              defaultConfig {
                  applicationId "io.github.cngmsunlocker"
                  minSdk 23
                  targetSdk 35
                  versionCode 10101
                  versionName "1.0.1-api101"
              }
          }

          dependencies {
              compileOnly "io.github.libxposed:api:101.0.1"
          }
          EOF_APP_GRADLE

          cat > app/src/main/AndroidManifest.xml <<'EOF_MANIFEST'
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <application
                  android:allowBackup="false"
                  android:description="@string/app_description"
                  android:extractNativeLibs="false"
                  android:hasCode="true"
                  android:label="@string/app_name"
                  android:theme="@style/AppTheme" />
          </manifest>
          EOF_MANIFEST

          cat > app/src/main/res/values/strings.xml <<'EOF_STRINGS'
          <resources>
              <string name="app_name">CN GMS Unlocker</string>
              <string name="app_description">Remove CN GMS feature flags from SystemConfig</string>
          </resources>
          EOF_STRINGS

          cat > app/src/main/res/values/styles.xml <<'EOF_STYLES'
          <resources>
              <style name="AppTheme" parent="android:style/Theme.Material.Light.NoActionBar" />
          </resources>
          EOF_STYLES

          cat > app/src/main/resources/META-INF/xposed/java_init.list <<'EOF_JAVA_INIT'
          io.github.cngmsunlocker.HookEntry
          EOF_JAVA_INIT

          cat > app/src/main/resources/META-INF/xposed/module.prop <<'EOF_MODULE_PROP'
          minApiVersion=101
          targetApiVersion=101
          staticScope=true
          EOF_MODULE_PROP

          cat > app/src/main/resources/META-INF/xposed/scope.list <<'EOF_SCOPE'
          system
          EOF_SCOPE

          cat > app/src/main/java/io/github/cngmsunlocker/HookEntry.java <<'EOF_JAVA'
          package io.github.cngmsunlocker;

          import android.util.Log;

          import java.lang.reflect.Constructor;
          import java.lang.reflect.Method;

          import io.github.libxposed.api.XposedModule;
          import io.github.libxposed.api.XposedModuleInterface.ModuleLoadedParam;
          import io.github.libxposed.api.XposedModuleInterface.SystemServerStartingParam;

          public final class HookEntry extends XposedModule {
              private static final String TAG = "CnGmsUnlocker";

              private static final String FEATURE_CN_GMS = "cn.google.services";
              private static final String FEATURE_SERVICES_UPDATER = "com.google.android.feature.services_updater";

              private static volatile boolean loggedOnce = false;

              @Override
              public void onModuleLoaded(ModuleLoadedParam param) {
                  logInfo("module loaded, api=" + getApiVersion()
                          + ", framework=" + getFrameworkName()
                          + " " + getFrameworkVersion());
              }

              @Override
              public void onSystemServerStarting(SystemServerStartingParam param) {
                  logInfo("system server starting");
                  hookSystemConfig(param.getClassLoader());
              }

              private void hookSystemConfig(ClassLoader classLoader) {
                  try {
                      Class<?> systemConfigClass = Class.forName(
                              "com.android.server.SystemConfig",
                              false,
                              classLoader
                      );

                      for (Constructor<?> constructor : systemConfigClass.getDeclaredConstructors()) {
                          constructor.setAccessible(true);
                          hook(constructor).intercept(chain -> {
                              Object result = chain.proceed();
                              Object thisObject = chain.getThisObject();
                              if (thisObject != null) {
                                  removeCnGmsFeatures(thisObject, "constructor");
                              }
                              return result;
                          });
                      }

                      for (Method method : systemConfigClass.getDeclaredMethods()) {
                          if ("getInstance".equals(method.getName())) {
                              method.setAccessible(true);
                              hook(method).intercept(chain -> {
                                  Object result = chain.proceed();
                                  if (result != null) {
                                      removeCnGmsFeatures(result, "after getInstance");
                                  }
                                  return result;
                              });
                          }

                          if ("getAvailableFeatures".equals(method.getName())) {
                              method.setAccessible(true);
                              hook(method).intercept(chain -> {
                                  Object thisObject = chain.getThisObject();
                                  if (thisObject != null) {
                                      removeCnGmsFeatures(thisObject, "before getAvailableFeatures");
                                  }
                                  return chain.proceed();
                              });
                          }
                      }

                      logInfo("SystemConfig hooks installed");
                  } catch (Throwable t) {
                      logError("SystemConfig hook failed", t);
                  }
              }

              private void removeCnGmsFeatures(Object systemConfig, String source) {
                  boolean ok1 = callRemoveFeature(systemConfig, FEATURE_CN_GMS);
                  boolean ok2 = callRemoveFeature(systemConfig, FEATURE_SERVICES_UPDATER);

                  if (!loggedOnce || !ok1 || !ok2) {
                      loggedOnce = true;
                      logInfo(source + ": removeFeature results: "
                              + FEATURE_CN_GMS + "=" + ok1 + ", "
                              + FEATURE_SERVICES_UPDATER + "=" + ok2);
                  }
              }

              private boolean callRemoveFeature(Object systemConfig, String featureName) {
                  try {
                      Method method = systemConfig.getClass().getDeclaredMethod("removeFeature", String.class);
                      method.setAccessible(true);
                      method.invoke(systemConfig, featureName);
                      return true;
                  } catch (Throwable t) {
                      logError("removeFeature failed for " + featureName, t);
                      return false;
                  }
              }

              private void logInfo(String message) {
                  log(Log.INFO, TAG, message);
              }

              private void logError(String message, Throwable throwable) {
                  log(Log.ERROR, TAG, message, throwable);
              }
          }
          EOF_JAVA

      - name: Build debug APK
        shell: bash
        run: |
          set -euo pipefail
          gradle :app:assembleDebug

      - name: Rename APK
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out
          cp app/build/outputs/apk/debug/app-debug.apk out/CnGmsUnlocker-api101-debug.apk
          ls -lh out

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: CnGmsUnlocker-api101-debug-apk
          path: out/CnGmsUnlocker-api101-debug.apk
          if-no-files-found: error
